import { pino } from 'pino';

// src/createLogger.js
var createLogger = (options) => {
  const pinoLogger = pino({
    name: options.name,
    level: options.level
  });
  return pinoLogger;
};

export { createLogger };
//# sourceMappingURL=index.js.map
//# sourceMappingURL=index.js.map