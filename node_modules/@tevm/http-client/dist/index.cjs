'use strict';

var viem$1 = require('@tevm/viem');
var viem = require('viem');

// src/createHttpClient.js
var createHttpClient = ({ url, name = `TevmClient:${url}` }) => {
  const { tevm } = viem.createPublicClient({
    name: `TevmRemoteClient:${url}`,
    transport: viem.http(url)
  }).extend(viem$1.tevmViemExtension());
  return {
    ...tevm,
    url,
    name
  };
};

exports.createHttpClient = createHttpClient;
//# sourceMappingURL=index.cjs.map
//# sourceMappingURL=index.cjs.map