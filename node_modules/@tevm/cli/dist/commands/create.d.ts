import { z } from 'zod';
import { type args } from '../utils/create-args.js';
import { type options } from '../utils/create-options.js';
type Props = {
    options: z.infer<typeof options>;
    args: z.infer<typeof args>;
};
export declare const description = "Create a new Ethereum account or smart contract";
export default function Create({ options, args: [defaultName] }: Props): import("react/jsx-runtime").JSX.Element;
export {};
