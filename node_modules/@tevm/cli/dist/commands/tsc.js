import { jsxs as _jsxs } from "react/jsx-runtime";
import { Text } from 'ink';
import { option } from 'pastel';
import zod from 'zod';
// Add command description for help output
export const description = "Compile TypeScript files with TEVM's TypeScript plugin";
export const options = zod.object({
    watch: zod.boolean().describe(option({
        description: 'Watch for changes',
        alias: 'w',
    })),
    project: zod
        .string()
        .optional()
        .describe(option({
        description: 'Path to tsconfig.json',
    })),
    noEmit: zod.boolean().describe(option({
        description: 'Do not emit outputs',
        alias: 'n',
    })),
});
export default function Tsc({ options }) {
    return (_jsxs(Text, { children: ["Running TypeScript compiler", options.project ? ` with config ${options.project}` : '', options.watch ? ' in watch mode' : '', options.noEmit ? ' (no emit)' : ''] }));
}
