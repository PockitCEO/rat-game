/**
 * Creates all template files needed for the editor
 * @param {string} actionName - The action name
 * @param {any} options - The options object
 * @param {Function} _createParams - Function to create params
 */
export function generateTemplates(actionName: string, options: any, _createParams: Function): {
    scriptTemplate: string;
    packageJson: string;
    pluginsTemplate: string;
    bunfigTemplate: string;
    tsconfigTemplate: string;
    readmeContent: string;
};
