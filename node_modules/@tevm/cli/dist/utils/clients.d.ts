/**
 * Determines if the given action name is a Viem action
 * @param {string} actionName - The name of the action
 * @returns {boolean} - True if it's a Viem action, false otherwise
 */
export function isViemAction(actionName: string): boolean;
export function loadViemClient(rpcUrl: string): Promise<Object | null>;
