import { Server } from 'node:http';
import { MemoryClient } from '@tevm/memory-client';
export declare function initializeServer({ port, host, chainId, verbose, fork, }: {
    port: number;
    host: string;
    chainId: string;
    fork?: string;
    forkBlockNumber: string;
    loggingLevel: string;
    verbose: boolean;
}): Promise<{
    client: MemoryClient;
    server: Server;
}>;
