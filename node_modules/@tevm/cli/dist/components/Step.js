import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Box, Text } from 'ink';
const DEFAULT_DESIRED_WIDTH = 16;
const formatName = (name, desiredWidth = DEFAULT_DESIRED_WIDTH) => {
    const leftWidth = Math.floor((desiredWidth - name.length) / 2);
    const rightWidth = Math.ceil((desiredWidth - name.length) / 2);
    return ' '.repeat(leftWidth) + name + ' '.repeat(rightWidth);
};
export const Step = ({ hide = false, isActive, activeContent, nonActiveContent, name, color, prompt, }) => {
    if (hide) {
        return null;
    }
    return (_jsxs(Box, { minHeight: 3, flexDirection: "column", children: [_jsxs(Box, { flexDirection: "row", gap: 2, children: [_jsx(Text, { bold: true, color: "black", backgroundColor: color, children: formatName(name) }), _jsx(Text, { children: prompt })] }), _jsx(Box, { paddingLeft: 18, children: isActive ? activeContent : nonActiveContent })] }));
};
