import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { relative } from 'node:path';
import { Box, Text } from 'ink';
import Spinner from 'ink-spinner';
import Table from './Table.js';
export const Creating = ({ store }) => {
    return (_jsxs(Box, { display: "flex", flexDirection: "column", children: [_jsx(Table, { data: [
                    {
                        name: store.name,
                        template: store.framework,
                        path: relative(process.cwd(), store.path),
                    },
                ] }), _jsx(Box, { children: _jsxs(Text, { children: [_jsx(Spinner, { type: "dots" }), " Creating project..."] }) })] }));
};
