import { jsx as _jsx } from "react/jsx-runtime";
import { Text } from 'ink';
import TextInput from 'ink-text-input';
import { Step } from './Step.js';
export const TextInputStep = ({ step, value, placeholder, onSubmit, onChange, ...stepProps }) => {
    return (_jsx(Step, { ...stepProps, prompt: step.prompt, nonActiveContent: _jsx(Text, { children: value === '' ? placeholder : value }), activeContent: _jsx(TextInput, { placeholder: placeholder, value: value, onChange: onChange, onSubmit: onSubmit }) }));
};
