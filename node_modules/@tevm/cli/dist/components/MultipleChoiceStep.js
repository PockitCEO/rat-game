import { jsx as _jsx } from "react/jsx-runtime";
import { Text } from 'ink';
import { SelectInput } from './SelectInput.js';
import { Step } from './Step.js';
export const MultipleChoiceStep = ({ multipleChoice, selectedChoice, onSelect, ...stepProps }) => {
    return (_jsx(Step, { ...stepProps, prompt: multipleChoice.prompt, nonActiveContent: _jsx(Text, { children: multipleChoice.choices[selectedChoice]?.label }), activeContent: _jsx(SelectInput, { items: Object.values(multipleChoice.choices), onSelect: (choice) => {
                onSelect(choice.value);
            } }) }));
};
