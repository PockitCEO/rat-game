import { jsxs as _jsxs, jsx as _jsx } from "react/jsx-runtime";
import { Box, Text } from 'ink';
const ActionTab = ({ actionName, interactive, rpcUrl, ActionComponent }) => {
    if (!interactive) {
        return (_jsxs(Box, { flexDirection: "column", children: [_jsxs(Text, { bold: true, children: [actionName, " Action"] }), _jsx(Text, { children: "Press Enter to use this action interactively" })] }));
    }
    // Only render the actual action component when in interactive mode
    return (_jsx(ActionComponent, { options: {
            interactive: true,
            rpc: rpcUrl,
            formatJson: true,
        }, args: [] }));
};
export default ActionTab;
