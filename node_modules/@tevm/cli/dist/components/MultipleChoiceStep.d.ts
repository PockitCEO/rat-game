type Choice = {
    label: string;
    value: string;
};
type MultipleChoice = {
    type: 'multiple-choice';
    prompt: string;
    stateKey: string;
    choices: Record<string, Choice>;
};
type Props = {
    name: string;
    isActive?: boolean;
    hide?: boolean;
    color: string;
    multipleChoice: MultipleChoice;
    selectedChoice: string;
    onSelect: (value: string) => void;
};
export declare const MultipleChoiceStep: React.FC<Props>;
export {};
