export type LogEntry = {
    id: number;
    timestamp: string;
    type: 'request' | 'response' | 'error';
    content: string;
};
export interface LogStore {
    logs: LogEntry[];
    nextId: number;
    addLog: (type: 'request' | 'response' | 'error', content: any) => void;
    clearLogs: () => void;
}
export declare const useLogStore: import("zustand").UseBoundStore<import("zustand").StoreApi<LogStore>>;
/**
 * Create a proxy around the request function that logs to Zustand store
 */
export declare function createLoggingRequestProxy(originalRequest: any, verbose: boolean): (...args: any[]) => Promise<any>;
