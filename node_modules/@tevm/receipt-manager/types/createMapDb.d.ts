/**
 * Mapping of database entry types to their numeric IDs for storage
 * Only append new items to the bottom of the list to remain backward compatible.
 * @type {Record<import('./MapDb.js').DbType, number>}
 */
export const typeToId: Record<import("./MapDb.js").DbType, number>;
export function createMapDb({ cache }: {
    cache: Map<import("@tevm/utils").Hex, Uint8Array>;
}): import("./MapDb.js").MapDb;
//# sourceMappingURL=createMapDb.d.ts.map