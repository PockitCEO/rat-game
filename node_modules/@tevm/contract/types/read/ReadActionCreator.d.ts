import type { AbiParametersToPrimitiveTypes, Address, ExtractAbiFunction, ExtractAbiFunctionNames, FormatAbi, Hex, ParseAbi } from '@tevm/utils';
/**
 * Utility type to get the value type of an object.
 * @template T - The object type.
 */
export type ValueOf<T> = T[keyof T];
/**
 * A mapping of view and pure contract methods to action creators.
 * This type provides a way to create type-safe read actions for contract methods.
 *
 * @template THumanReadableAbi - The human-readable ABI of the contract.
 * @template TAddress - The address of the contract (optional).
 * @template TCode - The runtime bytecode of the contract (optional).
 * @template TAddressArgs - Additional arguments for the address (derived from TAddress).
 *
 * @example
 * ```typescript
 * // Assuming we have a contract with a 'balanceOf' method
 * const balanceAction = MyContract.read.balanceOf('0x1234...')
 *
 * // Use the action with tevm
 * const balance = await tevm.contract(balanceAction)
 * console.log('Balance:', balance)
 * ```
 */
export type ReadActionCreator<THumanReadableAbi extends readonly string[], TAddress extends Address | undefined, TCode extends Hex | undefined, TAddressArgs = TAddress extends undefined ? {} : {
    address: TAddress;
    to: TAddress;
}> = {
    [TFunctionName in ExtractAbiFunctionNames<ParseAbi<THumanReadableAbi>, 'pure' | 'view'>]: (<TArgs extends AbiParametersToPrimitiveTypes<ExtractAbiFunction<ParseAbi<THumanReadableAbi>, TFunctionName>['inputs']> & any[] = AbiParametersToPrimitiveTypes<ExtractAbiFunction<ParseAbi<THumanReadableAbi>, TFunctionName>['inputs']> & any[]>(...args: TArgs) => {
        functionName: TFunctionName;
        humanReadableAbi: FormatAbi<[ExtractAbiFunction<ParseAbi<THumanReadableAbi>, TFunctionName>]>;
        abi: [ExtractAbiFunction<ParseAbi<THumanReadableAbi>, TFunctionName>];
    } & (TCode extends undefined ? {} : {
        code: TCode;
    }) & (TArgs['length'] extends 0 ? {} : {
        args: TArgs;
    }) & TAddressArgs) & {
        functionName: TFunctionName;
        humanReadableAbi: FormatAbi<[ExtractAbiFunction<ParseAbi<THumanReadableAbi>, TFunctionName>]>;
        abi: [ExtractAbiFunction<ParseAbi<THumanReadableAbi>, TFunctionName>];
    } & (TCode extends undefined ? {} : {
        code: TCode;
    }) & TAddressArgs;
};
//# sourceMappingURL=ReadActionCreator.d.ts.map